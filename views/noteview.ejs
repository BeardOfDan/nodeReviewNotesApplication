<!DOCTYPE html>
<html>

<head>
  <% include headerStuff %>
</head>

<body>
  <div class='container-fluid'>
    <% include pageHeader %>

      <div class='row'>
        <div class='col-xs-12'>
          <h3>
            <%= note ? note.title : '' %>
          </h3>
          <p>
            <%= note ? note.body : ''%>
          </p>
          <p>Key:
            <%= notekey %>
          </p>
        </div>
      </div>

      <% if (notekey) { %>
        <div class='row'>
          <div class='btn-group col-sm-12'>
            <a class='btn btn-defautl' rol='button' href='/notes/destroy?key=<%= notekey %>'>Delete</a>
            <a class='btn btn-defualt' role='button' href='/notes/edit?key=<%= notekey %>'>Edit</a>
          </div>
        </div>
        <% } %>
          <% include footer %>
</body>

</html>
